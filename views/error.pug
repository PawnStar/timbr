extends layout

block content
  .error
    .hero
      h1= err.message

      if err.stack
        .message
          pre= err.stack
      else
        .spacer

      h3 Sorry about that
      p= err.details || 'Looks like our server encountered an error.  Feel free to refresh the page and try again.'
